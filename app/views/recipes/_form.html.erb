Name: <%= f.text_field :name %><br>

<%= f.collection_check_boxes :ingredient_ids, Ingredient.all, :id, :name %>


<%= f.fields_for :ingredient do |i| %>

	<br>New Ingredients: <%= i.text_field :name %>

<% end %>
